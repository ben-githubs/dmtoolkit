# How to add a new module

1. Create a new sub (python) module with the relevant code.
2. Create a `Module` object.
3. Register the `Module` object in 'dmtoolkit/modules/registering.py'

Once a module is updated, the UI elements and settings page will automatically create the appropriate widgets.